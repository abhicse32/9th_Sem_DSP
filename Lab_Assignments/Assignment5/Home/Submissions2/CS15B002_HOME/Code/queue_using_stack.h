#ifndef QUEUE_USING_STACK_H
#define QUEUE_USING_STACK_H
#include "stack.h"
#include "List.h"
#include <stdio.h>
#include <stdlib.h>
void enqueue(stack* q,int n);
void dequeue(stack* q);
void size(stack* q);
void empty(stack* q);
#endif
