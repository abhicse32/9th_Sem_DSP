#ifndef STACK_H
#define STACK_H
#include "List.h"
#include <stdbool.h>
#include "stack.h"
#include "queue.h"

stack* queue_new()                               //Create a new queue


int dequeue(stack* )                            //Remove the front element from the queue

bool queue_is_empty(stack* )                    //Check if queue is empty


int queue_size(stack* )                         //Find the size of the queue

void queue_print(stack* )                       //Print queue element


#endif